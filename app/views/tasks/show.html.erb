<h1><%= @task.title %></h1>

<p><%= @task.details %></p>

<p><%= @task.completed? ? "This task is completed" : "This task is not completed yet" %></p>

<%= link_to "Update this task", edit_task_path(@task.id) %>
<%= link_to "Back to list", root_path %>
